    const SIDEBAR_STATE_KEY = "stemsplitter.sidebarCollapsed";
    const SIDEBAR_WIDTH_KEY = "stemsplitter.sidebarWidth";
    const SIDEBAR_MIN_WIDTH = 220;
    const SIDEBAR_MAX_WIDTH = 680;
    const MIN_VISIBLE_DURATION = 0.1;
    const ZOOM_PRESETS = [null, 300, 120, 60, 30, 10, 5, 2, 1, 0.5, 0.2, 0.1];
    const ZOOM_IN_FACTOR = 0.8;
    const ZOOM_OUT_FACTOR = 1.25;
    const PAN_DRAG_THRESHOLD = 4;
    const appShell = document.getElementById("app-shell");
    const mainPane = document.getElementById("main-pane");
    const sidebarToggle = document.getElementById("sidebar-toggle");
    const sidebarResizeHandle = document.getElementById("sidebar-resize-handle");
    const form = document.getElementById("split-form");
    const splitPanel = document.getElementById("split-panel");
    const projectListEl = document.getElementById("project-list");
    const projectListLoadingEl = document.getElementById("project-list-loading");
    const projectEmptyEl = document.getElementById("project-empty");
    const projectSearchInput = document.getElementById("project-search");
    const newSplitBtn = document.getElementById("new-split-btn");
    const newFolderBtn = document.getElementById("new-folder-btn");
    const newFolderRow = document.getElementById("new-folder-row");
    const newFolderInput = document.getElementById("new-folder-input");
    const newFolderConfirm = document.getElementById("new-folder-confirm");
    const newFolderCancel = document.getElementById("new-folder-cancel");
    const audioFileInput = document.getElementById("audio_file");
    const audioDropHint = document.getElementById("audio-drop-hint");
    const splitDropzone = document.getElementById("split-dropzone");
    const sourceUrlInput = document.getElementById("source_url");

    // Auto-expand textarea to fit content, collapse back when empty
    function _autoResizeUrlInput() {
      sourceUrlInput.style.height = "auto";
      sourceUrlInput.style.height = Math.min(sourceUrlInput.scrollHeight, 260) + "px";
    }
    sourceUrlInput.addEventListener("input", _autoResizeUrlInput);
    sourceUrlInput.addEventListener("paste", () => setTimeout(_autoResizeUrlInput, 0));

    const urlDedupBanner = document.getElementById("url-dedup-banner");
    const urlDedupMsg = document.getElementById("url-dedup-msg");
    const urlDedupOpenBtn = document.getElementById("url-dedup-open-btn");
    const urlDedupDismissBtn = document.getElementById("url-dedup-dismiss-btn");
    const sourceDownloadBtn = document.getElementById("source-download-btn");
    const urlActionInput = document.getElementById("url_action");
    const submitBtn = document.getElementById("submit-btn");
    const downloadMp3Btn = document.getElementById("download-mp3-btn");
    const card = document.querySelector(".card");
    const statusErr = document.getElementById("status-err");
    const statusOk = document.getElementById("status-ok");
    const progressSection = document.getElementById("progress-section");
    const progressFill = document.getElementById("progress-fill");
    const progressText = document.getElementById("progress-text");
    const cancelJobBtn = document.getElementById("cancel-job-btn");
    const projectLoadingPanel = document.getElementById("project-loading-panel");
    const stemProgressPanel = document.getElementById("stem-progress-panel");
    const mixerPanel = document.getElementById("mixer-panel");
    const lyricsPanel = document.getElementById("lyrics-panel");
    const lyricsList = document.getElementById("lyrics-list");
    const lyricsKaraokeView = document.getElementById("lyrics-karaoke-view");
    const lyricsEditView = document.getElementById("lyrics-edit-view");
    const lyricsAddLineBtn = document.getElementById("lyrics-add-line-btn");
    const lyricsPasteBtn = document.getElementById("lyrics-paste-btn");
    const lyricsPasteArea = document.getElementById("lyrics-paste-area");
    const lyricsPasteInput = document.getElementById("lyrics-paste-input");
    const lyricsPasteImportBtn = document.getElementById("lyrics-paste-import-btn");
    const lyricsPasteDismissBtn = document.getElementById("lyrics-paste-dismiss-btn");
    const lyricsViewToggle = document.getElementById("lyrics-view-toggle");
    const lyricMarkTransportBtn = document.getElementById("lyric-mark-transport-btn"); // may be null now
    const miniLyricMarkBtn = document.getElementById("mini-lyric-mark-btn"); // may be null now
    const lyricsMarkLeadBtn   = document.getElementById("lyrics-mark-lead-btn");
    const lyricsMarkChorusBtn = document.getElementById("lyrics-mark-chorus-btn");
    const lyricsMarkMusicBtn  = document.getElementById("lyrics-mark-music-btn");
    const lyricsMarkLeadBtnB   = document.getElementById("lyrics-mark-lead-btn-bottom");
    const lyricsMarkChorusBtnB = document.getElementById("lyrics-mark-chorus-btn-bottom");
    const lyricsMarkMusicBtnB  = document.getElementById("lyrics-mark-music-btn-bottom");
    const _allMarkBtns = () => [lyricsMarkLeadBtn, lyricsMarkChorusBtn, lyricsMarkMusicBtn, lyricsMarkLeadBtnB, lyricsMarkChorusBtnB, lyricsMarkMusicBtnB];
    const lyricsVocalsCanvas  = document.getElementById("lyrics-vocals-canvas");
    const lyricsVocalsPlayhead = document.getElementById("lyrics-vocals-playhead");
    const lyricsVocalsMarkLines = document.getElementById("lyrics-vocals-lyric-marks");
    const lyricsVocalsWaveWrap = document.getElementById("lyrics-vocals-wave-wrap");
    const stemProgressName = document.getElementById("stem-progress-name");
    const stemProgressBar = document.getElementById("stem-progress-bar");
    const stemProgressMsg = document.getElementById("stem-progress-msg");
    const mixerSection = document.getElementById("mixer-section");
    const exportBar = document.getElementById("export-bar");
    const tracksContainer = document.getElementById("tracks-container");
    const downloadMixBtn = document.getElementById("download-mix-btn");
    const downloadAllBtn = document.getElementById("download-all-btn");
    const exportProjectBtn = document.getElementById("export-project-btn");
    const importProjectBtn = document.getElementById("import-project-btn");
    const importProjectInput = document.getElementById("import-project-input");
    const playBtn = document.getElementById("play-btn");
    const playIcon = document.getElementById("play-icon");
    const pauseIcon = document.getElementById("pause-icon");
    const stopBtn = document.getElementById("stop-btn");
    const transportSeek = document.getElementById("transport-seek");
    const transportTime = document.getElementById("transport-time");
    const tempoSlider = document.getElementById("tempo-slider");
    const pitchSlider = document.getElementById("pitch-slider");
    const tempoResetBtn = document.getElementById("tempo-reset-btn");
    const pitchResetBtn = document.getElementById("pitch-reset-btn");
    const zoomSlider = document.getElementById("zoom-slider");
    const tempoValue = document.getElementById("tempo-value");
    const pitchValue = document.getElementById("pitch-value");
    const pitchLoading = document.getElementById("pitch-loading");
    const tempoLoading = document.getElementById("tempo-loading");
    const zoomValue = document.getElementById("zoom-value");
    const setLoopStartBtn = document.getElementById("set-loop-start");
    const setLoopEndBtn = document.getElementById("set-loop-end");
    const clearLoopBtn = document.getElementById("clear-loop");
    const loopStartInput = document.getElementById("loop-start-input");
    const loopEndInput = document.getElementById("loop-end-input");
    const applyLoopRangeBtn = document.getElementById("apply-loop-range");
    const loopToggleBtn = document.getElementById("loop-toggle-btn");
    const loopValue = document.getElementById("loop-value");
    const tempoBadgeBtn = document.getElementById("tempo-badge-btn");
    const pitchBadgeBtn = document.getElementById("pitch-badge-btn");
    const tempoBadgeVal = document.getElementById("tempo-badge-val");
    const pitchBadgeVal = document.getElementById("pitch-badge-val");
    const tempoPopover = document.getElementById("tempo-popover");
    const pitchPopover = document.getElementById("pitch-popover");
    let activeProjectId = "";
    let allProjects = [];
    let projectsTotal = 0;
    let projectsOffset = 0;
    const PROJECTS_PAGE_SIZE = 50;
    let activeProjectName = "";
    function _setActiveProjectName(name) {
      activeProjectName = name || "";
      if (miniPlayerTitle) miniPlayerTitle.textContent = activeProjectName;
      if (workspaceProjectName) workspaceProjectName.textContent = activeProjectName;
    }
    let activeTrackKey = "";
    let activeProjectMenuJobId = "";
    let activeProjectRenameJobId = "";
    let activeFolderRenameName = "";
    let activeFolderPickerJobId = "";
    let _activePortalMenu = null;   // menu element waiting to be moved into the portal
    let projectSearchTerm = "";
    let projectSortMode = localStorage.getItem("stemsplitter.sortMode") || "recent"; // "recent" | "alpha"
    let _isAdminUser = false;   // set from /api/v1/auth/session
    let _sessionUser = {};      // full user object from /api/v1/auth/session

