# Lite mode — browse, import, and play only. No splitting, no BPM/key analysis.
# Use this on servers where you only need to import exported projects and play stems.
pyyaml
tqdm
numpy<2
Flask>=3.0
Authlib>=1.3
requests>=2.31
